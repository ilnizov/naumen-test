import { ShowSuggestionsDirective } from './show-suggestions.directive';

describe('ShowSuggestionsDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowSuggestionsDirective();
    expect(directive).toBeTruthy();
  });
});
